apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "homework-miniapi.fullname" . }}-test-connection"
  labels:
    {{- include "homework-miniapi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "homework-miniapi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
